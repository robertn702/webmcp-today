{
  "version": 1,
  "domain": "news.ycombinator.com",
  "urlPatterns": ["*://news.ycombinator.com/*"],
  "title": "Hacker News",
  "description": "Read Hacker News through its official public API, plus comment, vote, and submit as the logged-in user. Write tokens (hmac, vote auth, fnid) are extracted from HN's own HTML pages; a page-shape change fails loudly. Write tools require an active news.ycombinator.com login session.",
  "minEngine": 1,
  "changelog": "Adds official public API read tools: hn_get_item and hn_list_children. Existing writes use HTML token extraction and form/query targets.",
  "api": {
    "baseUrl": "https://news.ycombinator.com",
    "auth": {
      "hmac": {
        "source": { "endpoint": "itemPage", "pattern": "name=\"hmac\" value=\"([^\"]+)\"" },
        "sendAs": { "in": "form", "name": "hmac" },
        "ttlSeconds": 300
      },
      "voteAuth": {
        "source": {
          "endpoint": "itemPage",
          "pattern": "vote\\?id={{itemId}}&(?:amp;)?how={{how}}&(?:amp;)?auth=([^&\"]+)"
        },
        "sendAs": { "in": "query", "name": "auth" },
        "ttlSeconds": 300
      },
      "fnid": {
        "source": { "endpoint": "submitPage", "pattern": "name=\"fnid\" value=\"([^\"]+)\"" },
        "sendAs": { "in": "form", "name": "fnid" },
        "ttlSeconds": 300
      }
    },
    "endpoints": {
      "itemPage": {
        "method": "GET",
        "path": "/item",
        "query": { "id": "{{itemId}}" }
      },
      "submitPage": {
        "method": "GET",
        "path": "/submit"
      },
      "comment": {
        "method": "POST",
        "path": "/comment",
        "form": { "parent": "{{itemId}}", "text": "{{text}}" },
        "auth": ["hmac"]
      },
      "vote": {
        "method": "GET",
        "path": "/vote",
        "query": { "id": "{{itemId}}", "how": "{{how}}", "goto": "item?id={{itemId}}" },
        "auth": ["voteAuth"]
      },
      "submit": {
        "method": "POST",
        "path": "/r",
        "form": { "title": "{{title}}", "url": "{{url}}", "text": "{{text}}" },
        "auth": ["fnid"]
      },
      "item": {
        "method": "GET",
        "baseUrl": "https://hacker-news.firebaseio.com",
        "path": "/v0/item/{{itemId}}.json",
        "returns": "id && {id: id, type: type, by: by, time: time, title: title, text: text, url: url, score: score, descendants: descendants, parent: parent, kids: kids, deleted: deleted, dead: dead, parts: parts, poll: poll}"
      },
      "children": {
        "method": "GET",
        "baseUrl": "https://hacker-news.firebaseio.com",
        "path": "/v0/item/{{itemId}}.json",
        "returns": "id && {id: id, type: type, kids: kids || `[]`}"
      }
    }
  },
  "tools": [
    {
      "name": "hn_get_item",
      "description": "Get a Hacker News story, comment, job, poll, or poll option by numeric ID through HN's official public API. Returns metadata, HTML text, direct child-comment IDs, and poll relationships when present.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "itemId": {
            "type": "string",
            "description": "Numeric HN item ID, from an /item?id=... URL or another HN tool result"
          }
        },
        "required": ["itemId"],
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": true },
      "execution": { "mode": "api", "endpoint": "item" }
    },
    {
      "name": "hn_list_children",
      "description": "List the direct child comment IDs of a Hacker News story or comment, in HN display order. Call hn_get_item for each child to read it; an empty kids list means no direct replies.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "itemId": {
            "type": "string",
            "description": "Numeric ID of the story or comment whose direct replies to list"
          }
        },
        "required": ["itemId"],
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": true },
      "execution": { "mode": "api", "endpoint": "children" }
    },
    {
      "name": "hn_comment",
      "description": "Post a comment on a Hacker News story, or reply to a comment, as the logged-in user. The item ID is the number in /item?id=... URLs. A successful post returns the thread page HTML.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "itemId": {
            "type": "string",
            "description": "Numeric ID of the story or comment to reply to, from /item?id=..."
          },
          "text": {
            "type": "string",
            "description": "Comment body (plain text; blank lines split paragraphs, links auto-link)"
          }
        },
        "required": ["itemId", "text"],
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": true },
      "execution": { "mode": "api", "endpoint": "comment" }
    },
    {
      "name": "hn_vote",
      "description": "Upvote (or retract an upvote on) a Hacker News story or comment, as the logged-in user. The item ID is the number in /item?id=... URLs. Fails loudly if the item is not votable (own post, expired, logged out).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "itemId": {
            "type": "string",
            "description": "Numeric ID of the story or comment to vote on, from /item?id=..."
          },
          "how": {
            "type": "string",
            "description": "up = upvote, un = retract a previous upvote",
            "enum": ["up", "un"]
          }
        },
        "required": ["itemId", "how"],
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": false },
      "execution": { "mode": "api", "endpoint": "vote" }
    },
    {
      "name": "hn_submit",
      "description": "Submit a new story to Hacker News as the logged-in user: a link post (title + url) or a text post like Ask HN (title + text). Provide exactly one of url or text. Duplicate URLs are resubmitted to the existing thread by HN.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "description": "Submission title (HN limit: 80 chars)" },
          "url": {
            "type": "string",
            "description": "Link to submit; omit for a text post. Provide exactly one of url or text"
          },
          "text": {
            "type": "string",
            "description": "Body for a text post (Ask HN etc.); omit for a link post. Provide exactly one of url or text"
          }
        },
        "required": ["title"],
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": true },
      "execution": { "mode": "api", "endpoint": "submit" }
    }
  ]
}
