{
  "name": "@webmcp-today/extension",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "mkdir -p .wxt/chrome-profile && wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare",
    "typecheck": "wxt prepare && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@webmcp-today/engine": "workspace:*",
    "@webmcp-today/schema": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wxt": "^0.20.6"
  }
}
