# Registry API the background script fetches configs from. WXT inlines any
# WXT_-prefixed var into import.meta.env at build time. Defaults to
# http://localhost:3000 in code if unset.
WXT_REGISTRY_API_URL="http://localhost:3000"
